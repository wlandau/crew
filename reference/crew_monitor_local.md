# Create a local monitor object.

Create an `R6` object to monitor local processes created by `crew` and
`mirai`.

## Usage

``` r
crew_monitor_local()
```

## See also

Other monitor:
[`crew_class_monitor_local`](https://wlandau.github.io/crew/reference/crew_class_monitor_local.md)
